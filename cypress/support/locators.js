const locators = {
    LOGIN: {
        TF_WORKSPACE : '#workspace',
        TF_USER: '#username',
        TF_PASS: '#password',
        BTN_ACCESS: '#submit_button'
    },
    MAIN: {
        IMG_LOGO: 'a.topbar-widget > img'
    }
}

export default locators;